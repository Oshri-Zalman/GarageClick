# ----- Server -----
PORT=3000
NODE_ENV=development

# ----- MySQL Database -----
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=garageclick
DB_CONNECTION_LIMIT=10

# ----- Auth (used in later steps) -----
JWT_SECRET=change_this_in_production
